# 開発環境の作り方 — 引き継ぎ用

このアプリを引き継ぐ人が、最初に読む文書です。
**5分で動かせます。** ビルドも `npm install` も要りません。

---

## 1. このアプリの成り立ち(最初に知っておくこと)

| | |
|---|---|
| 種類 | 静的なWebアプリ(PWA)。HTML・CSS・JavaScriptだけ |
| ビルド | **なし。** ブラウザがそのまま読めるファイルで構成されている |
| 依存パッケージ | **なし。** グラフも自前のSVG、フォントも端末のもの |
| サーバー | **なし。** データは利用者の端末の中(localStorage)だけ |
| 公開先 | GitHub Pages。`main` に push すると **30秒〜1分で本番に反映される** |
| AIの費用 | **かからない。** 利用者が自分のAPIキーを登録する方式(BYOK) |

**この4つの「なし」がこのアプリの設計の核**です。
便利そうに見えても、ライブラリやビルド工程を持ち込むと、この良さが崩れます。
足したくなったときは `HANDOVER.md` の「壊してはいけない前提」を先に読んでください。

---

## 2. 必要なもの

| | 用途 | 確認 |
|---|---|---|
| **Node.js 18以上** | 開発用スクリプト(サーバー・テスト) | `node --version` |
| **Google Chrome** | 操作テストとスクリーンショット | — |
| **Git** | 履歴とデプロイ | `git --version` |

Node.js が無くても**アプリ自体は動きます**(ブラウザで開くだけ)。
テストを走らせるときだけ必要です。

---

## 3. 動かす

```bash
git clone https://github.com/mameyompo-maker/nutrition-tracker.git
cd nutrition-tracker
npm start
```

`http://127.0.0.1:8080/` が開けます。ポートを変えたいときは `npm start -- 3000`。

> `index.html` をダブルクリックしても大半は動きますが、
> **オフライン対応(サービスワーカー)は `http://` 経由でないと有効になりません。**
> 動作を確認するときは必ず `npm start` を使ってください。

### 使ってみるにはAPIキーが要ります

初回設定で **「接続テスト」が成功しないと先へ進めません**(意図的な作りです)。
Google AI Studio で無料のキーを取れます(クレジットカード不要)。
手順はアプリの中の「取得のしかた」に入っています。

---

## 4. テストを走らせる

```bash
npm test
```

2段構えになっています。

| | 内容 | 所要 |
|---|---|---|
| `dev/check.mjs` | 構文、参照先の実在、**サービスワーカーのキャッシュ漏れ** | 数秒 |
| `dev/test.mjs` | ヘッドレスChromeで画面を実際に操作(90項目以上) | 1〜2分 |

```bash
npm run test:verbose   # 通った項目も全部出す
npm run check          # 静的チェックだけ(速い)
```

**失敗すると終了コード1を返す**ので、そのままCIで使えます。

### 画面を目で見る

```bash
npm run shot                  # 全画面を dev/screenshots/ に出力
npm run shot -- home trends   # 画面を指定
npm run shot -- entry share   # シートを開いた状態
npm run shot:dark             # ダークモード
```

---

## 5. テストの仕組み(直すときに読む)

`dev/` の中身:

```
dev/
├── lib.mjs        共通部分(サーバー、ブラウザ起動、テスト用コピーの組み立て)
├── check.mjs      静的チェック
├── test.mjs       操作テストの実行役
├── shot.mjs       スクリーンショット
└── harness/       ブラウザの中で走るテスト用スクリプト
    ├── seed.js      記録・体重・お気に入りを仕込む
    ├── probe.js     画面を操作して結果を集める  ← テストを足すのはここ
    ├── sheet.js     シートを開いた状態を作る(撮影用)
    ├── theme.js     配色の算出値を取り出す
    └── shot.html    実機幅(390px)で表示するための入れ物
```

動き方:

1. `lib.mjs` がアプリ一式を**一時フォルダにコピー**し、`index.html` に
   ハーネスのスクリプトを差し込む(**プロジェクト本体は汚さない**)
2. その場に簡易サーバーを立てる
3. ヘッドレスChromeで開く
4. `probe.js` が画面を操作し、**結果を `POST /__result` で送り返す**
5. 受け取った時点でブラウザを終了させ、結果を表示する

> **4の「送り返す」が要点です。** 以前は `--dump-dom` と
> `--virtual-time-budget` で待っていましたが、ページ側が `canvas.toBlob` の
> ような非同期処理をしているとブラウザが終了しなくなり、原因の分かりにくい
> ハングを何度も起こしました。**結果を送らせて、こちらから閉じる**のが確実です。

### テストを足す

`dev/harness/probe.js` に1行足すだけです。

```js
ok("説明", 条件, 失敗時に出したい値);
```

`sel()` / `click()` / `wait()` が使えます。`state` や `Storage` など、
アプリ側の関数にもそのまま触れます。

### 踏んだ罠(繰り返さないために)

- **`--user-data-dir` は毎回新しい場所にする。** 付けないと無言で何も出力せず、
  使い回すと固まる(`lib.mjs` が自動でそうしています)
- **テスト用のコピーではサービスワーカーの登録を潰してある。**
  でないと前回キャッシュした古いファイルを返してきて、
  直したはずの不具合が直っていないように見える
- **ヘッドレスのウィンドウ幅には下限(およそ500px)がある。**
  `--window-size=390` としてもレイアウトは約500px幅で組まれる。
  実機の幅で見たいときは `shot.html`(幅390pxのiframe)を使う

---

## 6. 本番に出す

```bash
git add -A
git commit -m "変更の内容"
git push origin main
```

**push した時点で本番です。** 30秒〜1分で
https://mameyompo-maker.github.io/nutrition-tracker/ に反映されます。

出す前に必ず `npm test` を通してください。
大きな変更は Pull Request にすると、CIの結果を見てから出せます。

### アセットを足したときは

**`sw.js` の `ASSETS` に追加し、`CACHE_NAME` の版を上げる**のを忘れないでください。
忘れると利用者の端末に古いファイルが残り続けます。
`npm run check` がこの入れ忘れを検出します。

---

## 7. 次に読むもの

| 文書 | 中身 |
|---|---|
| `README.md` | アプリとしての説明。何ができるか、栄養の根拠、無料で使う手順 |
| `HANDOVER.md` | **開発の勘どころ。** 壊してはいけない前提、実測で分かったこと、踏んだ罠 |
| `PROMPT_v4.md` | 次にやる変更の仕様(配色の刷新、カレンダー、献立提案など) |
| `COST_AND_SCALING.md` | どの機能に費用がかかるか、拡大したときの見通し |

**変更に入る前に `HANDOVER.md` を読んでください。**
CORSの可否、Geminiの混雑時の挙動、栄養の基準値の出典など、
調べ直すと時間がかかることがまとめてあります。

---

## 8. ライセンス

MIT。自由に使い、変更し、再配布できます(`LICENSE` を参照)。
