# 食事栄養トラッカー v4 — 実装依頼プロンプト

> **使い方**: この文書をそのままクラウドのコーディングエージェントに貼る。
> 第1章〜第2章は必ず最初に読ませる前提条件。第3章以降が実際の変更依頼で、
> **フェーズA〜Eは1セッションで通してよいが、フェーズF(SNS)は別セッションに分ける**
> (アーキテクチャが根本から変わり、レビューすべき判断が多いため)。

---

## 1. 対象と現状

| 項目 | 内容 |
|---|---|
| リポジトリ | https://github.com/mameyompo-maker/nutrition-tracker (public) |
| 公開URL | https://mameyompo-maker.github.io/nutrition-tracker/ |
| 現行版 | v3.0 |
| 構成 | 静的なHTML/CSS/JavaScriptのみ。ビルド工程なし。GitHub Pages |
| デプロイ | **main への push が即本番反映**(Pagesが自動ビルド、30秒〜1分) |

### ファイルの役割

- `index.html` — 画面の骨格。タブバー、シートの置き場、スクリプト読み込み
- `style.css` — 全スタイル。CSS変数(トークン)で色・角丸・フォントを一元管理
- `app.js` — 画面描画と全イベント処理。状態は `state` オブジェクト1つ
- `nutrition.js` — 食事摂取基準(2025年版)の表と1日の必要量の計算、`NUTRIENT_META`(21項目)
- `charts.js` — グラフ。**外部ライブラリを使わずインラインSVGを文字列で組む**
- `exif.js` — 撮影情報(EXIF)の読み取りと、写る範囲の実寸の計算
- `storage.js` — localStorage の読み書きと日付ヘルパー
- `api.js` — 各AIサービスへの解析リクエスト。プロバイダ定義と手順書もここ
- `icons.js` — インラインSVGアイコン集
- `manifest.json` / `sw.js` / `icon-*.png` — PWA用

**作業前に必ず `HANDOVER.md` を読むこと。** CORSの可否、Geminiの503挙動、
ヘッドレスChromeでの検証手順と踏んだ罠など、調べ直すと時間がかかる実測結果がある。

---

## 2. 壊してはいけない前提

以下は今回の変更でも維持する。**やむを得ず崩す場合は、勝手に進めず必ず先に相談すること。**

1. **絶対に無料で運用できること。** 開発者側に費用が発生しない構成を守る
   (※ フェーズFのSNSだけはこの前提と衝突する。第9章で改めて扱う)
2. **BYOK方式**。開発者のAPIキーを持たず、利用者が自分のキーを登録してブラウザから
   直接AIを呼ぶ。キーは端末内にのみ保存し、開発者のサーバーを経由させない
3. **外部ライブラリ・Webフォント・CDNをゼロに保つ。** グラフも自前のインラインSVG。
   「軽いこと」が要件なので、npm パッケージやチャートライブラリを導入しない
4. **ビルド工程を増やさない。** ブラウザがそのまま読めるファイルだけで構成する
5. **栄養の目標値は厚生労働省「日本人の食事摂取基準(2025年版)」からの転記。**
   記憶や2020年版で書き換えない(B12は目安量4.0µg、ビタミンDは9.0µg)。
   `nutrition.js` の表を触ったら、参照体重でEER表と28通り一致するか検算し直す
6. **EXIFのGPSは読まない・送らない。** 食事の写真から自宅や勤務先が割れるため
7. **アセットを増やしたら `sw.js` の `ASSETS` と `CACHE_NAME` の版を必ず上げる**
   (現在 `nutriapp-shell-v7` → 今回 `v8` に)
8. **医学的助言ではない旨の注記を各所から消さない**

---

## 3. フェーズA — 端末の設定によらず見た目を固定する

### 背景

現在は `@media (prefers-color-scheme: dark)` でダークモードに追従している。
これをやめ、**端末がダークモードでもライトモードでも同じ配色で表示する**。

### やること

1. `style.css` から `@media (prefers-color-scheme: dark)` のブロックを**すべて削除**する
   (トークン定義側と、`.toast` などの個別上書きの両方。取り残しがないか
   `prefers-color-scheme` で全文検索して確認する)
2. `:root` に `color-scheme: only light;` を指定する。
   **`light` ではなく `only light` にすること。** Android Chrome の「自動ダークテーマ」は
   `light` だとページを強制的に暗転させることがあるが、`only light` なら抑止できる
3. `index.html` の `<meta name="color-scheme">` を `content="only light"` にし、
   **`<meta name="theme-color">` からメディアクエリ付きの2行を削除して1行にする**
   (新しい背景の緑を指定)
4. `manifest.json` の `theme_color` / `background_color` も同じ緑に揃える
5. `<meta name="apple-mobile-web-app-status-bar-style">` を再検討する。
   背景が濃い緑になるので `black-translucent` が合うか実機で確認する

### 受け入れ条件

- 端末をダークモードにしても、ライトモードと同一の配色で表示される
- 入力欄・チェックボックス・`<select>` の矢印など、ブラウザ標準の部品が暗転しない
- 検証: ヘッドレスChromeに `--force-dark-mode` を付けても付けなくても、
  `getComputedStyle` で取った `--bg` と `body` の背景色が一致する

---

## 4. フェーズB — 配色を緑と温かい色に刷新する

### 方針

- **背景をアプリ全体の緑にする。** その上に生成り(温かい白)のカードを置く
- 栄養・食べもの・自然を感じさせる、暖色寄りの配色にまとめる
- 色数はむやみに増やさない。**トークン(CSS変数)経由でのみ色を使い、
  値をセレクタに直接書かない**

### 差し色について(依頼者に確認済み・2026-08-22)

原案にあった差し色は **ひまわり色(温かい黄)** で確定。下の `--sun` がそれにあたる。
達成の表示・注目させたい箇所・緑の上に置くアクセントに使う。

### 配色トークン(たたき台。実装しながら微調整してよい)

```css
:root {
  color-scheme: only light;

  /* 面 */
  --bg:          #37613C;  /* アプリ全体の背景。葉の緑 */
  --bg-deep:     #2B4E2F;  /* タブバー・上部バーなど、一段沈めた緑 */
  --surface:     #FFFCF4;  /* カードの面。生成り(温かい白) */
  --surface-2:   #F5EEDD;  /* カードの中の一段沈んだ面 */
  --fill:        rgba(55, 97, 60, 0.08);   /* 入力欄などの薄い塗り */
  --fill-strong: rgba(55, 97, 60, 0.16);

  /* 文字(生成りのカードの上で使う) */
  --text:   #22301F;
  --text-2: #58684F;
  --text-3: #8A9682;

  /* 文字(緑の背景の上で使う) */
  --on-bg:   #FFFCF4;
  --on-bg-2: rgba(255, 252, 244, 0.72);

  /* 操作色・強調色 */
  --accent:      #2F5D37;  /* カードの上のボタン・リンク */
  --accent-soft: rgba(47, 93, 55, 0.10);
  --sun:         #F2B233;  /* ひまわり。達成・注目・緑の上のアクセント */
  --sun-soft:    rgba(242, 178, 51, 0.16);

  /* 状態 */
  --warn:   #C2701A;
  --danger: #B3392C;
}
```

### データの色(P/F/C)

v3.0では Apple のシステムカラー(青・黄・ティール)を当てていたが、**新しい配色に合わせて
暖色寄りの自然な3色に置き換える**。グラフとバーとドットにのみ使い、装飾には使わない。

| 栄養素 | 案 | 由来 |
|---|---|---|
| たんぱく質 | `#B0512F` | 赤茶(肉・豆) |
| 脂質 | `#E0A32E` | 山吹(油・種実) |
| 炭水化物 | `#7C9A3C` | 若草(穀物) |

### 守ること

- **本文は 4.5:1、グラフや境界などのUI要素は 3:1 以上のコントラスト比を確保する。**
  特に「緑の背景の上の文字」と「生成りのカードの上の淡い文字」は実測して確認する
- P/F/Cの3色は、生成りの面の上で互いに見分けがつくこと(色覚の多様性を考え、
  **色だけに頼らず必ずラベルか数値を併記する**。v3.0はすでにそうなっている)
- 角丸(10/12/16/22px)、余白の取り方、字組みの階層は現行を踏襲する
- Webフォントは引き続き読み込まない

### 受け入れ条件

- 全画面(ホーム/記録/トレンド/履歴/設定/各シート/初回設定)で、
  読めない・沈む・浮きすぎる箇所がないこと
- アプリアイコンとPWAのスプラッシュ(`background_color`)も新配色に合わせて更新
- 色の値がセレクタに直接書かれていない(`grep` で `#` の直書きを洗って確認する)

---

## 5. フェーズC — 五大栄養素とそれ以外を分ける

### 背景

現在は21項目すべてが並ぶ場面があり、情報が多すぎて要点が埋もれている。

### やること

**五大栄養素**(たんぱく質・脂質・炭水化物・ビタミン・ミネラル)を主表示にし、
**個別のビタミン・ミネラルは「詳細」タブにしまう**。

#### ホーム画面

- 主表示: エネルギー(リング) / たんぱく質 / 脂質 / 炭水化物 に加えて、
  **「ビタミン」「ミネラル」を各1行の要約**にする
  - 例: `ビタミン  9項目中 6項目が目標達成` + 達成率のバー
  - ビタミン群 = A, C, D, E, B1, B2, B6, B12, 葉酸(9項目)
  - ミネラル群 = カルシウム, 鉄, 亜鉛, マグネシウム, カリウム(5項目)
  - 食物繊維・食塩相当量・飽和脂肪酸は五大栄養素に含まれないので、
    **「その他」として主表示に残す**(食塩は摂りすぎ管理で重要なため)
- ビタミン/ミネラルの行をタップすると、その群の内訳だけが開く

#### 解析結果の入力フォーム・記録の編集シート

- **セグメントコントロールで「主要 / 詳細」を切り替える**
  - 主要 = エネルギー・たんぱく質・脂質・炭水化物・食物繊維・食塩相当量
  - 詳細 = 残り15項目
- 切り替えても入力途中の値を失わないこと(**DOMの表示切り替えだけで行い、
  再描画しない**。オンボーディングの `gotoObStep` と同じ考え方)

### 受け入れ条件

- ホームの初期表示で画面に出る栄養の行が **8行以下**
- 「詳細」に切り替えて数値を編集し、「主要」に戻してから保存しても、
  詳細側の編集が保存されること(ここが壊れやすいので必ずテストする)

---

## 6. フェーズD — 次の食事の献立をすすめる

### やること

解析のあと、および ホームから、**残りの栄養に基づいて次の食事の候補を提案する**。

- 提案は **明示的なボタンを押したときだけ** 生成する
- 入力としてAIに渡すもの:
  - 今日の残り(目標 − 摂取)のうち **不足が大きい上位5項目**
  - 残りのエネルギー
  - 現在の時刻帯(次が朝食か昼食か夕食か間食か)
  - **その日すでに食べたものの名前**(同じ献立を勧めないため)
  - 目標(維持/増量/減量)
  - **除外したい食材**(下記)
- 出力(JSONで受け取り、`api.js` の既存の正規化と同じ作法で扱う):

```json
{
  "suggestions": [
    {
      "name": "鮭のホイル焼きと小松菜のおひたし",
      "reason": "不足しているビタミンDとカルシウムを一度に補えます",
      "covers": ["vitaminD", "calcium", "protein"],
      "kcal": 380,
      "protein": 28, "fat": 12, "carb": 34,
      "effort": "簡単"
    }
  ],
  "note": "…"
}
```

### 無料枠を守るための制約(重要)

Geminiの無料枠は1日あたりおおむね1,500回。**画面を開くたびに自動で呼ぶと使い切る。**

- 提案は**手動のボタンでのみ発火**させる
- 生成結果は `localStorage` に **その日ぶんをキャッシュ**し、
  記録が増えて内容が変わったときだけ再生成できるようにする
- 連打防止(生成中はボタンを無効化)
- 失敗時は既存の503リトライとエラー文言の仕組みをそのまま使う

### 安全のために必要なこと(必ず入れる)

- **プロフィールに「避けたい食材・アレルギー」の自由入力欄を追加**し、
  提案のプロンプトに必ず含める。**アレルギーのある食材を勧めるのは実害につながる**
- 提案には「一般的な目安であり、医学的な助言ではない」旨を明示する
- 提案をそのまま記録に入れられるようにしてよいが、**その場合も推定値であることを示す**

### 受け入れ条件

- ボタンを押さない限りAPIが呼ばれない(ネットワークログで確認)
- 同じ日に2回目を押しても、内容が変わっていなければキャッシュを返す
- アレルギー欄に入れた食材が提案に出てこない(実際にAPIを叩いて確認する)

---

## 7. フェーズE — 履歴をカレンダーで遡れるようにする

### やること

履歴タブを**月カレンダーを主役にした画面**に作り替える。

- 月のグリッド(日〜土の7列)。各日のマスに:
  - 記録がある日は**達成度が分かる小さなリングまたは塗り**を出す
  - 記録がない日は薄いまま
  - 今日は縁取りで示す
- 日をタップすると、その日の記録一覧が下に開く(またはシートで開く)
- **前月/翌月へ移動**できる(ボタン + 左右スワイプ)。未来の月へは進ませない
- 「今日へ戻る」ボタン
- **月の合計・平均を出す**(平均エネルギー、記録した日数)
- **既存の料理名検索は残す**(カレンダーの上に検索欄を置き、
  入力があるときは検索結果一覧に切り替わる、という現在の作法を踏襲)

### 実装上の注意

- 外部のカレンダーライブラリを使わない。`storage.js` の日付ヘルパーを拡張して自前で組む
- **月初の曜日と月の日数の計算を取り違えないこと。** 月をまたぐ移動、
  うるう年、月末(1/31から翌月へ移動するなど)を必ずテストする
- 記録が1件もない月を開いても壊れないこと
- 描画コストに注意。月を切り替えるたびに全ログを走査せず、
  日付キーの索引を1度作って使い回す

### 受け入れ条件

- 数か月前まで遡って、記録のある日に到達できる
- 2月・うるう年・月末をまたぐ移動で表示がずれない
- 記録が0件の月でエラーにならない

---

## 8. フェーズF — SNS機能(別セッションで扱う)

> **このフェーズだけは、他と性質が根本的に違う。**
> 着手する前に第9章の判断を依頼者と合意すること。合意なしに実装を始めない。

### 依頼内容

- 食べたものの写真を他の人と共有できるようにする
- いいね・コメントなど、X や Instagram のようなSNS機能をつける

### 最小機能セット(合意できた場合)

| 機能 | 内容 |
|---|---|
| アカウント | メール+パスワード、または外部ログイン。表示名とアイコン |
| 投稿 | 記録から「共有する」で投稿。写真・料理名・エネルギー・任意で栄養サマリ・本文 |
| タイムライン | 新着順。フォロー中のみ / すべて の切り替え |
| いいね | 1投稿1ユーザー1回。取り消し可 |
| コメント | 文字数上限あり。削除可 |
| フォロー | フォロー/フォロワー |
| プロフィール | 自分の投稿一覧 |
| **通報・ブロック** | **必須。第9章の理由による** |
| **公開範囲** | **投稿ごとに 公開 / フォロワーのみ / 非公開 を選べること** |
| 退会 | アカウントとデータの削除 |

### 技術的に必ず守ること

1. **投稿する画像からEXIFを完全に除去する。**
   このアプリは解析時に `resizeImageToBase64()` でキャンバス再エンコードしており、
   **その過程でEXIFは落ちる**。投稿には**必ずこの再エンコード後の画像を使い、
   利用者が選んだ元ファイルを直接アップロードしない**。
   元ファイルにはGPSが残っており、自宅や勤務先が割れる。ここは絶対に緩めない
2. **APIキーは絶対にサーバーへ送らない。** BYOKの前提は維持する。
   解析は従来どおりブラウザから利用者自身のAIへ直接投げる
3. **食事の記録本体はこれまでどおり端末内に置く。**
   投稿したものだけがサーバーへ行く。「記録=公開」にしない
4. 投稿画像は**アップロード前に長辺720px程度・JPEG品質0.75まで圧縮**し、
   1枚あたり150KBを超えないようにする(第9章のコスト試算の前提)。
   **さらに一覧表示用に長辺240px・約20KBのサムネイルも作って2枚保存し、
   タイムラインではサムネイルだけを配る。** 拡大したときに初めて720pxを読む。
   これだけで転送量が約73%減り、支えられる人数が3.7倍になる
   (詳細は COST_AND_SCALING.md 第4章)
5. **投稿画像には保持期間(既定90日)を設け、期限切れを自動削除する。**
   画像の保管量は消さない限り永久に積み上がり、時間が経つだけで必ず破綻する。
   食事の記録本体は端末に残るので、消えるのは共有した画像だけ
6. サーバー側で**認可(自分の投稿しか消せない等)を必ず強制する**。
   クライアント側のチェックだけに頼らない。リポジトリはpublicなので、
   **公開されるのは anon キーだけにし、権限は必ずサーバー側の規則で縛る**
7. レート制限(連投・大量いいね)を入れる

---

## 9. フェーズFで先に決めるべきこと(依頼者の判断が要る)

### 9.1 「絶対に無料」との衝突

SNSにはサーバー・データベース・画像置き場・認証が要る。
GitHub Pages は静的ファイルしか置けないので、**これまでの「サーバーを持たないから
無料」という構図はSNSには使えない**。無料枠のあるサービスを使っても上限があり、
**利用が育てば有料化は避けられない。**

### 9.2 プライバシーの説明が変わる

現在 README と アプリ内の「このアプリについて」には
「**データはすべてこの端末のブラウザ内に保存され、開発者には送信されません**」とある。
投稿機能を入れると、**投稿した写真と本文についてはこれが成り立たなくなる。**
文言の全面的な書き換えと、利用規約・プライバシーポリシーの用意が要る。

### 9.3 他人の投稿を預かる責任

利用者が投稿する場になる以上、誹謗中傷・権利侵害・不適切な画像への対応が要る。
日本では **情報流通プラットフォーム対処法(旧プロバイダ責任制限法、2025年4月1日施行)**
があり、削除申出への迅速な対応などが定められている。1週間以内の判断・通知といった
**厳しい義務の対象は「大規模プラットフォーム事業者」**なので個人の小規模サービスに
直ちに全部がかかるわけではないが、**通報窓口・削除・ブロックの仕組みは最初から要る**。
最新の要件は実装前に必ず確認すること。

### 9.4 選択肢と費用の見積もり

> **詳細な費用の内訳・節約の手立て・拡大したときの見通しは
> [COST_AND_SCALING.md](COST_AND_SCALING.md) にまとめてある。着手前に必ず読むこと。**
> 特に「一覧はサムネイルだけ配る」「画像を90日で自動削除する」の2つは、
> 支えられる人数を桁で変えるので、**最初から設計に織り込むこと**。

前提: 投稿画像1枚 約120KB(長辺720px・JPEG q0.75)。

#### 案1: バックエンドを持たない「共有だけ」

`navigator.share()`(Web Share API)で、写真と栄養サマリを X・Instagram・LINE など
**既存のSNSアプリへ渡す**。共有用の画像(写真+数値を重ねたカード)をキャンバスで生成する。

- 費用: **0円のまま。今の構成を一切変えない**
- プライバシーの説明も変えなくてよい
- **できないこと: アプリ内のいいね・コメント・タイムライン**

#### 案2: Supabase で最小SNS

- 無料枠: データベース 500MB / **画像 1GB** / 月間アクティブ 50,000人 /
  **転送 5GB/月** / **7日間アクセスがないとプロジェクトが一時停止**
- 認証・権限(RLS)が最初から付いており、**最も速く作れる**
- 詰まる順番:
  - 画像1GB ÷ 120KB ≒ **投稿 約8,700枚**。1日30投稿なら 8,700 ÷ 30 ≒ 290日で、
    **10か月ほどで満杯**
  - **転送5GB/月のほうが先に来る。** タイムラインで1人が1日50枚見ると
    50 × 120KB ≒ 5.9MB/日。**利用者30人で 5.9 × 30 × 30日 ≒ 5.1GB/月となり、
    30人の時点でもう無料枠を超える。** 100人なら約17GB/月
  - つまり**「投稿が貯まって詰まる」より先に「見られて詰まる」**。
    SNSは見る回数が投稿の何倍にもなるので、ここが効いてくる
- 一時停止も、使い始めの時期には実際に困る

#### 案3: Cloudflare(Workers + D1 + R2)

- 無料枠: リクエスト 10万/日 / D1 5GB / **R2 10GB** / **R2 は転送料(egress)が無料**
- 画像10GB ÷ 120KB ≒ **投稿 約87,000枚**。1日30投稿なら約8年ぶん
- **転送が無料なので、閲覧が何倍に増えても費用が増えない。**
  案2で最初に詰まる箇所が、この案では最初から存在しない
- 代わりに**認証を自前で書く必要があり、案2より手間がかかる**

#### 案4(除外): Firebase

Cloud Storage が **2026年2月3日以降、請求先アカウント(Blaze)必須**になり、
**カード登録なしでは画像を置けない**。「カード登録不要で無料」という条件に合わないため、
今回は候補から外す(無料枠自体は残っており、超えなければ請求は発生しない)。

### 9.5 推奨する進め方

1. **まず案1(共有だけ)を入れる。** 費用ゼロ・法的な負担ゼロで「人に見せる」体験は成立する。
   ここで実際にどれだけ共有されるかを見る
2. **アプリ内のいいね・コメントが本当に必要だと分かってから案3(Cloudflare)で作る。**
   画像の転送料が無料である一点が、この用途では効いてくる
3. 認証を書く手間を惜しむなら案2(Supabase)で始め、
   **画像だけ R2 に置く**折衷も取れる

---

## 10. 全体の受け入れ条件

- `node --check` が全JSファイルで通る
- ヘッドレスChromeでの操作テスト(`HANDOVER.md` の手順)が全項目通る。
  **v3.0時点の80項目を壊していないことを確認し、今回の変更ぶんのテストを足す**
- 端末幅 390px で、どの画面も横スクロールが出ず、ボタンのラベルが折り返さない
- ダークモードの端末でも配色が変わらない
- 外部ライブラリ・Webフォントの読み込みがゼロのまま
- `sw.js` の `CACHE_NAME` を上げ、`ASSETS` に新規ファイルを追加してある
- `README.md` と `HANDOVER.md` を更新してある

## 11. 作業の順番

```
A(テーマ固定) → B(配色) → C(栄養素の整理) → E(カレンダー) → D(献立提案)
                                                        ↓
                                          F(SNS)は第9章の合意後に別セッション
```

AとBは同時に進めるのが自然(どちらもトークンを触るため)。
DはAPIを叩くので、他が安定してから最後に回す。

---

## 12. 進め方の作法

- **1フェーズごとにコミットを分ける。** まとめて1コミットにしない
- **main への push は即本番反映**なので、テストが通ってから push する
- 判断に迷ったら、勝手に前提を崩さず質問を返す。特に第2章の8項目
- 変更のたびに `HANDOVER.md` の該当箇所を直す。次に触る人が同じ罠を踏まないように、
  **踏んだ罠と実測結果を残す**
