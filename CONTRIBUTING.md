# 開発に参加する

**GitHubのアカウントがあれば、誰でも変更を提案できます。**
招待も、特別な権限も要りません。

提案されたものが勝手に反映されることはありません。
中身を確認して、こちらが取り込みを決めます。

---

## 1. 全体の流れ

| | 誰が | すること |
|---|---|---|
| 1 | あなた | このリポジトリを **Fork**(自分用の複製を作る) |
| 2 | あなた | 複製の側で直す |
| 3 | あなた | **Pull request** を出す(「こう直しました」と伝える) |
| 4 | 自動 | テストが走る。結果が Pull request の画面に出る |
| 5 | 管理者 | 中身を見て **Merge**(取り込み)を判断する |

Merge されると数十秒で
https://mameyompo-maker.github.io/nutrition-tracker/ に反映されます。

---

## 2. ブラウザだけで直す(かんたん)

文章の直しや、CSSを1行変える程度ならこれで足ります。

1. リポジトリの右上の **Fork** を押す
2. 自分の複製で、直したいファイルを開いて鉛筆マークを押す
3. 直したら **Commit changes**
4. 上の **Contribute** → **Open pull request**
5. 何を直したか一言書いて **Create pull request**

---

## 3. 手元で直す(おすすめ)

動作を確かめてから出せます。**ビルドも `npm install` も要りません。**

```bash
# 1. Fork してから、自分の複製を持ってくる
git clone https://github.com/<あなたのユーザー名>/nutrition-tracker.git
cd nutrition-tracker

# 2. 作業用の枝を切る
git switch -c fix-something

# 3. 動かしながら直す
npm start       # http://127.0.0.1:8080/ が開ける

# 4. 出す前に必ず通す
npm test

# 5. 送る
git add -A
git commit -m "何を直したか"
git push origin fix-something
```

push すると GitHub に「Compare & pull request」のボタンが出ます。

環境の詳細は **`SETUP.md`** にあります。

---

## 4. 出す前に

- **`npm test` を通してください。** 通らないものは取り込めません
- **`HANDOVER.md` の「壊してはいけない前提」を読んでください。**
  外部ライブラリを足さない、ビルド工程を増やさない、など、
  このアプリの土台にあたる決めごとが書いてあります
- ファイルを足したときは **`sw.js` の `ASSETS` に追加**してください
  (`npm run check` が入れ忘れを教えてくれます)

---

## 5. 先に相談してほしいこと

次にあたるものは、**出す前に Issue で一声かけてください。**
費用や責任が発生したり、土台を動かしたりする部分です。

- **SNS機能**(投稿の共有、いいね、コメント)
  → `PROMPT_v4.md` 第9章。判断が要る事項がまとまっています
- サーバーや外部サービスを使う変更(このアプリは現在サーバーを持ちません)
- 外部ライブラリ、Webフォント、CDNの導入
- 栄養の基準値の変更(厚生労働省2025年版に準拠しています)

一方、次はそのまま提案してもらって大丈夫です。

- 配色・レイアウト・表示の改善
- 不具合の修正
- `PROMPT_v4.md` に書いてある変更(第9章を除く)
- 文章やドキュメントの直し

---

## 6. 見るところ

Pull request では次を見ています。答え合わせではなく、目安です。

- テストが通っているか
- 上の「壊してはいけない前提」に触れていないか
- 動作が重くなっていないか
- 何を直したのかが説明から分かるか

**はじめての Pull request でも歓迎します。**
うまくいかないところがあれば、途中の状態のまま出してもらって構いません。

---

## 7. ライセンス

MIT。Pull request を出した時点で、その内容も同じ条件で使えるものとして扱います。
